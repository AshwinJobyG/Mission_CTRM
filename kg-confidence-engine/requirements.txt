# kg-confidence-engine — dependencies grow phase by phase.

# Phase 1: corpus (stdlib only)
python-dotenv>=1.0             # auto-loads .env for credentials

# Phase 2: retrieval
numpy>=1.24
rank-bm25>=0.2.2
sentence-transformers>=2.2.0   # intended embedder (all-MiniLM-L6-v2)

# Phase 3: graph
networkx>=3.0

# Phase 4: decision synthesis
anthropic>=0.40                 # optional: Anthropic backend
langchain-openai>=0.2          # optional: company-hosted OpenAI-compatible (ION) backend

# Phase 5/7: eval + plots
matplotlib>=3.7   # numpy already pulled in above

# Phase 8: demo UI
streamlit>=1.36
pyvis>=0.3
